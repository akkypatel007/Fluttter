import 'package:flutter/material.dart';

class MyPackages extends StatelessWidget {
  const MyPackages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('MyPackages')),
    );
  }
}
