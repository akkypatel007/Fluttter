import 'package:flutter/material.dart';

class MyAssessmentReports extends StatelessWidget {
  const MyAssessmentReports({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('MyAssessmentReports')),
    );
  }
}
