import 'package:flutter/material.dart';

class RefersAndEarn extends StatelessWidget {
  const RefersAndEarn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('RefersAndEarn')),
    );
  }
}
