import 'package:flutter/material.dart';

class ScanQRcode extends StatelessWidget {
  const ScanQRcode({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('ScanQRcode')),
    );
  }
}
