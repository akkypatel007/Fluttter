import 'package:flutter/material.dart';

class GiveUsFeedBack extends StatelessWidget {
  const GiveUsFeedBack({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('GiveUsFeedBack')),
    );
  }
}
